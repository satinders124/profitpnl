import type { Metadata } from "next";
import { ResourceShell, GuideSection } from "@/components/resources/ResourceShell";
const title = "Trader Benchmark Report";
const description = "A living benchmark framework for trader performance metrics: win rate, expectancy, profit factor, drawdown, mistakes, and consistency.";
export const metadata: Metadata = { title: `${title} | ProfitPnL`, description, alternates: { canonical: "/reports/trader-benchmark-report" } };
const rows = [["Win rate", "40–55%", "Context matters more than raw accuracy"],["Profit factor", "1.2–2.0+", "Below 1 is negative, above 1.5 is healthier"],["Average R", "+0.10R to +0.50R", "Small positive expectancy compounds"],["Max drawdown", "Strategy dependent", "Must be survivable emotionally and financially"],["Most common leak", "FOMO / rule breaks", "Behavior is often more important than entries"]];
export default function Page(){return <ResourceShell title={title} description={description}><GuideSection title="Benchmark framework"><p>ProfitPnL will publish anonymized aggregate trader benchmarks as the dataset grows. Until then, this page defines the metrics serious traders should compare against themselves month over month.</p><div className="overflow-hidden rounded-2xl border border-line"><table className="w-full text-left text-sm"><tbody>{rows.map(([a,b,c])=><tr key={a} className="border-b border-line last:border-0"><td className="px-4 py-3 font-bold text-txt">{a}</td><td className="px-4 py-3 font-mono2 text-gold">{b}</td><td className="px-4 py-3 text-muted2">{c}</td></tr>)}</tbody></table></div></GuideSection><GuideSection title="Why this matters"><p>Original benchmark data becomes one of the strongest SEO and backlink assets in trading because competitors can copy calculators, but they cannot copy proprietary anonymized performance research.</p></GuideSection></ResourceShell>}
